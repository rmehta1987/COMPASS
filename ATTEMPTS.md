# ATTEMPTS
(one entry per red check.sh: date · item · attempt n/3 · what was tried · which step went red · why)
- 2026-09-04 · item 12 · attempt 1/3 · pipeline/ledger.py append-only JSONL + summary + verify · RED at step 2 (pytest): verify() built Ledger(run_dir) whose run_id defaulted to the directory name while rows carried the run_id they were written with, so a run whose id differs from its directory name never verifies · approach for attempt 2: the ledger's run_id is read from its rows when they exist; the directory name is only the default for a new ledger; verify checks all rows agree
- 2026-09-04 · item 13a · attempt 1/3 · Critique + GenerationEnv + critiques/revision/generation on HypothesisRecord · RED at step 2 (pytest): tests/test_causal_structure.py pinned the record's top-level key set to {artefact, structure} at item 13; 13a adds critiques, revision, generation. That pin doing its job is the point of the seam; attempt 2 re-applies the same change and updates the pin deliberately to the five keys
- 2026-09-04 · item 15a · attempt 1/3 · pipeline/run.py driver + tests + PairLike estimability/requires_derivation made read-only properties (a frozen ResolvedPair failed the settable-attribute check) · RED at step 2: mypy 59 -> 60 (test_mypy_count_does_not_increase); one new error in the driver, see the next line for which; approach for attempt 2: fix that one annotation, no ceiling touched
  the error: pipeline/run.py main(): ClaudeCliBackend is not a structural Backend (no chat member), the same gap generate/live_specifier.py:296 already carries; attempt 2 types that one local as Any
- 2026-09-04 · item 15c · attempt 1/3 · live run b2-20260904 (48 pairs, k=5, workers 5, launched 12:38) · RED at pair 16/48 at 14:29: one sample's `claude -p` exited 1 with an empty stderr inside `ClaudeCliBackend._run` -> `reason` -> `specify_once` -> the thread pool re-raised -> `pipeline.run.run` let it propagate and the process died; 15 ledger rows (10 emitted, 5 discarded), no summary.json, nothing committed; partial run moved to the scratchpad (b2-partial), never stamped or tagged · cause of the exit itself unknown (empty stderr; possibly a transient CLI/API failure under five concurrent calls) · approach for attempt 2: the driver retries the pair once after a pause and, on a second backend failure, appends a `backend_error` ledger row (new OUTCOMES member) and continues; anything that is not the backend's own RuntimeError still stops the run; then relaunch as b3-20260904
- 2026-09-04 · item 15c · attempt 2, process slip (gate not involved) · the commit-stamp-tag script guarded "no parked file staged" with `test "$(... | rg -c parked || true)" = "0"`; rg -c prints NOTHING on zero matches, so the test compared "" to "0", the first && chain stopped before commit 1, and the next line committed the unstamped artefacts under the stamp message (703a177), pushed, and tagged baseline-3dc8415eccfe-b3-20260904 there · fixed forward, never amended: d73676a is the real stamp (tree_sha 703a177), tag baseline-3dc8415eccfe-b3-20260904-stamped points at it; the unsuffixed tag is left in place (a pushed ref is not moved) and superseded · lesson: never build a guard on a command whose empty output means success; test the count with `grep -c` semantics or an explicit `if`
