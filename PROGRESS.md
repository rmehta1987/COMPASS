# PROGRESS
(one line per green commit: date · item · sha · what the check proved)
- 2026-09-04 · item 1 · check.sh v1 · steps 0-6 (key unreachable, build hash pin, pytest 743 passed with 4 file ignores + 4 node-id deselects, ruff/mypy vs ceilings read from tests, retrieval_eval, smoke ALL PASS) · seeded pytest failure turned step 2 red in 6 s · 1m32s
- 2026-09-04 · item 2 · pipeline/retrieval_record.py (RequestSnapshot, Hit, RetrievalRecord; pydantic, frozen, extra=forbid) · 11 tests: JSON round trip lossless for resolved and abstained, tuples/None survive, wording fields absent, abstention invariants refuse 5 inconsistent shapes · pipeline/ added to mypy files · 754 passed, ruff 228, mypy 59 · GREEN
- 2026-09-04 · item 3 · pipeline/retrieve.py (load_retriever, load_template, retrieve, reproduce, --reproduce CLI) + nearest_key on RetrievalRecord · python -m pipeline.retrieve --reproduce: R@1 0.643 (144/224), abstained 15, equals smoke_test EXPECTED[I] read from the file · FINDING: retriever.search() topk broke 5 exact ties (stem_option_dup duplicates) differently from the acceptance test's argmax, 3 of them on the gold target -> 141/224 = 0.629 through the raw path; adapter applies lowest-target-id-among-ties, retriever.py untouched · 11 new tests on a fake retriever · 765 passed · GREEN
- 2026-09-04 · item 4 · check.sh step 6: python -m pipeline.retrieve --reproduce must exit 0 (R@1 through the adapter == smoke_test EXPECTED[I]); smoke moves to step 7 · seeded: TIE_K=1 was caught earlier by pytest (step 2); a second seed the unit tests are blind to (abstentions counted as misses) turned step 6 red at 0.634 vs 0.643 · GREEN
- 2026-09-04 · item 5 · pipeline/strata.py (Strata.from_targets/from_retriever/unmeasured/of; classifier reused by path from src/char_strata.py) + Hit.stratum/unmeasured_stratum, adapter wires it · reproduced from the bundle + prereg: ses_employment 16/15, insurance_access 14/14, cancer_screening 5/5, demographics 26/17 = 61 targets / 51 constructs with zero gold rows; unclassified (158 targets, survey admin) also zero and flagged · 7 new tests, bundle tests skip when targets.json is withheld · 772 passed · GREEN
- 2026-09-04 · item 6 · pipeline/canary.py C1 (diabetes age -> m2:Q5.7, chronic_condition, measured, margin +0.1297), C2a-e (PM2.5, ADI, census tract, biospecimen, genetic ancestry all ABSTAIN, margins -0.075..-0.378), C3 (neighborhood income level + instance 'median household income' -> m1:Q5.4 household income, ses_employment UNMEASURED, margin +0.0021 exactly as the brief; raw text without the instance abstains at -0.050) · check.sh step 7; smoke moves to step 8 · seeded C1 key flip turned step 7 red (6/7) · 5 unit tests on the evaluator · 777 passed · GREEN
- 2026-09-04 · item 7 · pipeline/intake.py parse_request(text, role, timeframe) -> Intake(request, query, content_words, specific, notes); grammar = the template's rendering read backwards (construct[: instances][ [timeframe] ]); population never set; specificity note cites QUERY_EXPANSION.md 0.493 (1-2 words) vs 0.676 (4+), never rewrites · canaries now build requests through intake, so check.sh step 7 (7/7 hold) is the bundle-backed acceptance · 11 unit tests incl. every canary request reproduces through intake · 791 passed · GREEN
- 2026-09-04 · item 8 · pipeline/auto_intake.py requests_for(cand) -> (exposure, outcome) from the construct's stem; resolve_pair compares the hit's dict_construct_key (new on Hit; differs from construct_key on 2/1353 targets) with the funnel's construct_key; RequestSnapshot.source user|instrument · measured on the worked frame (6x64, 256 live): 6/6 exposures, 62/64 outcomes, 248/256 both; misses m2:Q5.15, m2:Q5.64 abstain on 21-word grid stems · FRAME_BOTH_FLOOR 248 · check.sh step 8 (--pair m3:Q16.1 m2:Q5.8 --frame); smoke moves to 9 · 5 unit tests on a fake · 796 passed · GREEN
- 2026-09-04 · item 9 · pipeline/gate.py: gate(cands, allow_unestimable) asks env.tools.estimate_n per pair; blocked_on = per_item_non_missing_counts (every set) + module_co_completion_counts (cross-module), the names estimate_n itself uses · worked frame: without the flag 0/256 pass and both exports named; with it 256/256 pass, every one estimability=blocked_no_metadata (the marker item 15 requires; no unmarked bypass exists) · check.sh step 9 runs both modes; smoke moves to 10 · 5 tests incl. monkeypatched computed_from_counts opens the gate on its own · 801 passed · GREEN
- 2026-09-04 · item 10 · agent/specifier.py: the existing _Pair Protocol promoted to PairLike (read-only exposure/outcome/pair_id properties, estimability, requires_derivation; _Anchor gains stem_text, is_group); specify, specify_once, user_prompt, prompt_hash and generate/live_specifier.run_identity annotate pair: PairLike · pipeline/resolved_pair.py: ResolvedPair from two RetrievalRecords + the dictionary's constructs, records ride in .retrieval, refuses abstained/unknown/swapped-role records · prompt byte-identical to the Candidate for the same anchors; a ResolvedPair goes through specify_once's real path (scripted backend, gate=missing_calls like the Candidate test) · 8 tests · 809 passed, ruff 226, mypy 59 (rose to 68 mid-item from the Protocol; fixed by read-only properties, not by widening) · GREEN
- 2026-09-04 · item 11 · pipeline/artefact.py: SpecifierArtefact = ProtocolSpecification dump + retrieval{exposure,outcome} + one VariableProvenance per named variable (exposure, outcome, adjusted, excluded, undetermined); anchors carry the full RetrievalRecord, covariates the Specifier named through tools carry source=specifier_tool and no record (none selected them; a fabricated one would say otherwise) · emit() refuses a record whose variable anchor is outside the pair's construct · redact(): quoted_wording and instrument-sourced request/query -> sha256:<hex>, idempotent, verifiable with the dictionary; user-typed text kept · tested on the worked-example fixture record + real constructs · 815 passed · GREEN
- 2026-09-04 · item 12 · pipeline/ledger.py: append-only ledger.jsonl per run (LedgerRow: pair, both construct keys, both strata, estimability, outcome in emitted|refused|no_valid_record|gate_blocked|discarded, protocol_id/record_hash/artefact, note, ts); RunSummary derived from rows only, total_generated_this_run == len(rows) by construction; verify(run_dir) fails on a stale summary, an edited (non-contiguous) ledger, a mixed run id or a missing summary · attempt 2/3 after attempt 1 went red on run-id-from-directory (ATTEMPTS.md) · 8 tests · 823 passed · GREEN
- 2026-09-04 · item 13 · pipeline/causal_structure.py: CausalStructure (design from unit_of_analysis: participant -> cross_sectional, visit/participant-year -> repeated_measures; typed Node/Edge; edges per CausalRole from EDGES_BY_ROLE: confounder C->X,C->Y; cause_of_exposure_only C->X; precision C->Y; proxy C->X,C->Y 'proxies'; mediator X->C,C->Y; descendants; the four no-edge roles isolated) + adjustment_set · pipeline/hypothesis.py: HypothesisRecord = artefact + structure, build(p, pair), to_json/from_json, redacted() · derived on the worked-example fixture record: 4 adjusted (2 confounders, 1 precision, 1 proxy), hypothesised edge first, no prose field in the structure · 6 tests · 829 passed · GREEN
- 2026-09-04 · item 13a · pipeline/hypothesis.py: Critique (source, category in confounding|measurement|identification|feasibility, statement, grounding_key, severity in blocking|major|minor, resolved) and critiques=() / revision=0 / generation=None on HypothesisRecord; stamped(env) · pipeline/generation_env.py: GenerationEnv (key_present, key_fetchable, tree_sha, tree_clean, branch; clean_for_scoring) and stamp(repo, require_pushed) measured by git: key_fetchable is true on a present scoring-key ref, a reachable key blob, OR a wildcard fetch refspec; measured here: key_present False, key_fetchable False, branch ralph-loop · attempt 1 went red on item 13's top-level key pin, updated deliberately (ATTEMPTS.md) · 7 tests incl. a temp-repo refspec test · 836 passed · GREEN
- 2026-09-04 · item 14 · pipeline/validators.py: temporality (mediator under cross_sectional -> blocking identification critique; descendant_of_exposure / confounder_or_mediator -> major), dag_consistency (hypothesised edge present, acyclic, adjusted nodes touch an anchor and are not on the X->Y path), cell_counts (analytic_n vs the design's own smallest_detectable_effect.at_n; silent when either is unknown), measurement_level (bare grid-battery anchor needs a derivation; free-text anchor); validate/apply/blocking/rejected_note; every critique source=validator:<name> through the item-13a seam, revision untouched · on the worked-example fixture record: m3:Q16.3 mediator under cross-sectional -> blocking, m1:Q4.1 -> major, dag consistent, measurement ok (derivation over the grid battery) · 9 tests incl. seeded cycle / removed edge / on-path adjustment / small n / bare grid anchor / free text · 845 passed · GREEN
- 2026-09-04 · item 15a · pipeline/run.py: narrow_frame (medication + reproductive_hormonal exposures x chronic_condition outcomes through the funnel), subset(seed), run(cands, backend, resolver, ...) -> gate -> resolve -> ResolvedPair -> run_identity + specify -> validators.apply -> HypothesisRecord.redacted() under artefacts/<run_id>/ + one ledger row per gated pair (gate_blocked / discarded at intake / refused / no_valid_record / emitted / discarded by a blocking critique) + summary; stamp_run(run_dir, env) refuses an unclean env; CLI with --limit/--seed/--k/--allow-unestimable/--frame-only · PairLike.estimability/requires_derivation made read-only properties so a frozen ResolvedPair satisfies it · attempt 1 red on mypy 59->60 (ClaudeCliBackend not a structural Backend, the gap live_specifier already carries), attempt 2 types that local Any (ATTEMPTS.md) · 4 tests: scripted backend end to end, the fixture record discarded by validator:temporality with a redacted artefact, no model call without the flag, seeded subset, stamp refusal · 849 passed · GREEN
- 2026-09-04 · item 15b · benchmark/baseline_score.py: takes artefact PATHS only; refuses (fatally, all or none) an unstamped artefact, a stamp with key_present/key_fetchable true or a dirty tree, a stamp naming a sha other than --sha, an unredacted artefact, a record retrieved under another dictionary, a path set that is not exactly the ledger's emitted set or that spans two run dirs · match rule: paper outcome key (outcome_keys_on_record) ∈ {hit.key, members, construct_key, dict_construct_key} of the hypothesis's outcome record AND the paper's exposure_terms resolved through the deployed retriever share a key with its exposure record · four numbers N, M (= ledger emitted count), N/M, ledger total_generated_this_run + by_outcome, strata, GenerationEnv, paper coverage (with outcome key / exposure resolved / matched), abstained terms, verdicts · contamination_check --live and input_leakage halt before any file is read; unearned_assertions is advisory · key-side modules imported inside functions only (AST-tested) so it loads here · render() writes BASELINE.md carrying the brief's qualifier verbatim · 12 tests on an injected PaperKey table and a cue-driven FakeRetriever · attempt 1 · 861 passed · GREEN
